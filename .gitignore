# Build output
dist/
build/
!build/sidecar.spec
*.exe
*.msi

# Fetched by setup.sh - too large to commit
.venv/
models/
assets/fonts/
!assets/commons/
*.onnx

# Generated media (samples are kept, see exception below)
*.mp4
*.wav
!samples/**/*.mp4
!samples/**/*.wav

# Python
__pycache__/
*.py[cod]

# Node
node_modules/

# Editors / OS
.vscode/
.idea/
Thumbs.db
.DS_Store
.venv-clone/
assets/voice/

# Pending: pushing workflow files needs the `workflow` OAuth scope,
# which this token lacks. Run `gh auth refresh -s workflow`, then
# remove these two lines and commit .github/workflows/build.yml.
.github/workflows/
